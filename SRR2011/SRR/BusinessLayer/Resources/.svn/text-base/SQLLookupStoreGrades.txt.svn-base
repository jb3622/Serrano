SELECT
	SUBSTRING(SKEY, 2) as Id,
	TRIM(SNAM) || ' (' || TRIM(SUBSTRING(SKEY, 2)) || ')' AS Description,
	TRIM(SNAM) AS AltDescription
FROM
	IPSTRTB
WHERE
	SKEY IN (SELECT ALSTGRP FROM DSALSTG)
ORDER BY
	SKEY