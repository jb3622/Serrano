SELECT
	h.ICLS As CLASS, 
	h.IVEN AS VENDOR, 
	h.ISTY AS STYLE, 
	h.ICLR AS COLOUR, 
	h.ISIZ AS SIZE, 
	h.IUPD AS UPC
FROM
	IPITHDR h
	JOIN IPISETS s
	ON h.ICLS = s.KCLS
	AND h.IVEN = s.KVEN
	AND h.ISTY = s.KSTY
	AND h.ICLR = s.KCLR
	AND h.ISIZ = s.KSIZ
WHERE
	h.ISET = 'Y'